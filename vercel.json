{
  "version": 2,
  "buildCommand": "npm ci && npm run build --prefix packages/web-ui",
  "outputDirectory": "packages/web-ui/dist/apps/web-ui",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
